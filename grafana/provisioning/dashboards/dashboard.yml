apiVersion: 1

providers:
  - name: 'SkyWatcher'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards  # 컨테이너 내부 경로